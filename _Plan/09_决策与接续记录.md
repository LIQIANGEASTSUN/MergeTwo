# 决策与接续记录

用于后续多次讨论或多 Agent 接续。先读本页和 README，再按主题读专项文档；不把“建议”“旧实现事实”“用户确认”混在一起。当前版本 v0.3，日期 2026-09-23。

## 用户已确认

| 编号 | 决策 | 影响 |
| --- | --- | --- |
| D01 | 先独立验证核心棋盘，再接入活动和外围系统 | M1／M2 先行；M3／M4 后续讨论 |
| D02 | 沿用当前项目 Profile，本地先完成规则与存档 | 不恢复旧服务器协议；先核对当前宿主 Profile 接入 |
| D03 | 首期售出／删除撤销，其它撤销和确定性回放后续讨论 | 单次专用撤销凭据，不做通用操作历史 |
| D04 | 本次二合不做订单，删除方案中的相关内容 | 移除相关设计、配置接入、实施步骤、验证用例和待讨论项 |

本次任务明确要求先理解参考资料、讨论方案并生成 `_Plan` 文档。这是当前工作的授权范围；用户尚未要求开始玩法实现。

## 当前推荐，尚非最终决策

| 编号 | 推荐 | 理由／关联 |
| --- | --- | --- |
| A01 | 独立二合模块，候选 `Assets/Game/MergeTwo` | 不牵动三合实现；保留参考包，见 02 |
| A02 | 采用 A／H 的 Command→OperationBatch→Graphic 核心契约 | 同步规则、显式顺序、可独立验证；各项语义见 02 |
| A03 | 补完整逻辑成功观察点与结构化故障出口 | 在表现前发布存档；避免把 bool 当成功收据，见 02／03 |
| A04 | Logic 自有状态，ProfileAdapter 发布独立稳定快照 | 减少 Profile／UI／逻辑多方写入，见 03 |
| A05 | 新 UGUI 绑定，先简单反馈再接旧特效 | 旧 Prefab 并非可直接运行，见 06 |
| A06 | 测试棋盘和二合专用测试余额先验证 | 真实 Profile 仍接入，宿主商业货币后置，见 01／03 |
| A07 | 固定段按实例一次，完整产出要求足够空格 | 更接近 R 前段与清晰费用语义，仍需明确裁定，见 05 |
| A08 | 无必要不提通用框架，不用全量 Entity／Function 树 | 小棋盘直接容器与规则易理解，见 02／04 |
| A09 | Function＋独立 Effect 数据与规则处理，保留必要业务状态 | 用效果组合表达锁等附加影响，统一能力查询；不建立两条业务写入链，见 10／11 |

ProjectIdea 文档中的“固定核心”等规范文字描述它自己的架构契约，不自动成为用户对新二合的不可更改指令。若决定使用该核心，应该保持并验证其现有语义；若本次决定改变，要明确记录差异及影响，不能一边声称沿用、一边悄悄改变失败／重入含义。

## 待讨论问题

这些问题按阶段解决，**不要求用户一次回答全部**。阅读和文档工作不被它们阻塞；依赖某条规则的实际编码才需要先裁定。

| 编号 | 问题与推荐出发点 | 决定时机 |
| --- | --- | --- |
| Q01 | 独立代码放 `Assets/Game/MergeTwo`、以 UGUI 验证是否合适？首个样例建议人工明确配置，随后再接旧 7×9 地图。 | M0，开始建代码／资源前 |
| Q02 | 是否接受整体架构 A02–A04：保留参考执行语义，新增完整成功提交出口与故障关闭，Logic→Profile 独立快照？ | M0，影响骨架与存档 |
| Q03 | 固定输出是每实例一次还是每轮重复？是否扣费／计次数？新合成生成器继承或重置哪些状态？非末轮切换后是否等首次点击再计时？ | M1 采用简单配置；完整规则在 M2 前 |
| Q04 | 多颗空间不足全拒绝还是部分产出？锁定产出阈值统计什么？主合成解锁与气泡／衍生求落点的先后？ | M2 对应能力前 |
| Q05 | 生成器保护统计哪些容器／类型？撤销时冷却自然流逝还是冻结？收入已花掉能否撤销？撤销记录是否跨关闭？ | M2 售出／撤销前 |
| Q06 | 气泡是否可拖动／换位？隐藏／退出是否清选择并处理已到期气泡？推荐关闭前归一化、重开不恢复选择；隐藏的实例是否持续调度另定。 | M1 定生命周期；M2 定气泡 |
| Q07 | 被动生成 0.1 秒是业务间隔还是演出间隔？是否离线补产？推荐首期不补离线产量。 | M2 被动生成前 |
| Q08 | M1 测试余额是否独立于真实宿主钱包？正式体力／金币如何接宿主？快照载荷使用哪种项目现有生成／序列化方式？ | M0／M1 定验证方案；正式经济 M3／M4 |
| Q09 | 仓库可收哪些状态、是否暂停？待领取队列的身份与领取顺序如何确定？ | M3 前，另做模块讨论 |
| Q10 | 时间服务使用当前项目哪一入口？首期可信时间／本机 UTC 如何选，时钟回拨如何处理？ | M1 计时前 |
| Q11 | 是否按 10 的推荐试验 Function＋Effect 组合？哪些锁附着 Cell／Item，效果如何继承／解除？保留哪些局部业务阶段？ | M0 模型定稿前；用户提出研究方向，尚未批准删除全部 State |

当前优先讨论 **Q11 的概念与宿主归属**，再与 Q02 执行边界、Q08 存档方式对齐；随后用“生成一次→合成一次→关闭重开”的具体状态例子确认 Q03。其余问题随模块推进。

## 已发现的重要事实

- 旧包实现为 Lua 与历史 UI／服务依赖，不能直接作为当前项目的新 C# 模块启动。
- 旧生成主流程单颗输出，固定段游标复用轮内次数；覆盖锁概率字段未在主流程执行。
- 旧克隆概率、衍生权重和气泡序列化存在静态可见问题；列在 01，不直接修改旧证据。
- 旧售卖显示方法为空；ShearsFunction 实际升级目标，与“剪刀拆分”的文字说明不一致。
- 当前三合的 MergeSpawn 由 Graphic ACK 推进，不能用它证明“动画失败不影响逻辑结果”。
- HomeHub 的 Apply 故障不回滚，Consumer 失败不撤回已登记 Operation；二合的经济与快照必须按真实契约组织。
- 参考包本轮审计为 FAIL，14 个 Atlas／Material 既有哈希差异；历史报告 PASS 不能代表当前状态。

完整证据和阅读边界在 00。本轮未运行游戏／Unity 测试，未对整个大目录逐行审计。

## 接续入口

所有相对路径以 `/Users/betta/Company/Projects/meatloaf_client/client` 为根。

1. 阅读 `Assets/MergeTwo/_Plan/README.md`、本页与 00，确认 D01–D04 和当前版本。
2. 架构讨论读 02、03，再对照 `/Users/betta/Company/Projects/ProjectIdea/核心玩法逻辑表现分离/Document/玩法逻辑与表现分离_实现规范.md` 的实际章节。
3. 业务讨论先读 01 的 R／C 编号，按 00 导航到原始 Lua／DOCX；有冲突时保留两方证据。
4. 找到本轮主题对应的 Q，收到用户明确答复后追加 D 编号，再修改正文、样例与验收。不要把 Agent 之间达成的一致写成用户已确认。
5. 若准备实际编码，先定位新的局部规则／skill、工作树变化和对应项目依赖；按 08 的下一步执行，不重做已经完成的调研或顺手修旧包。

可复制的接续说明：

> 请先阅读 Assets/MergeTwo/_Plan/README.md、09_决策与接续记录.md 和 00_参考资料与证据.md。当前是新二合玩法讨论稿，已确认独立核心棋盘、当前 Profile、本期仅售出／删除撤销，本次不做订单。请围绕我这次指定的问题核对原始资料，区分事实／建议／确认，更新对应专项文档及决策记录。除非我明确开始实施，先继续设计，不改玩法代码。

## 多 Agent 讨论的分工建议

这是未来需要并行时的交接方式，本轮没有创建独立 Agent。

| 主题 | 建议负责文件 | 独立任务边界 |
| --- | --- | --- |
| 核心契约与生命周期 | 02、06 | 对照 A／H 验证执行、异常、取消和退役语义 |
| 业务和配置 | 01、04、05 | 逐条解决 R／L 差异，提供可观察的规则例子 |
| Profile 与外围 | 03、07 | 核对真实注册／生成／存档链，梳理容器与经济边界 |
| 主协调 | README、00、08、09 | 维护用户决策、阶段顺序和证据，检查跨文档一致性 |

开始并行前分配文件所有权；同一状态模型／Operation 契约的修改先反馈主协调，不让两个 Agent 同时发明不同版本。结论要带文件和方法证据；本地阅读、静态审计、运行验证分别报告。

## 变更记录

| 版本／日期 | 变更 |
| --- | --- |
| v0.1／2026-09-23 | 首轮来源梳理、差异清单、架构／状态／业务分模块建议、实施与验证顺序；登记用户 D01–D03，保留待讨论项 |
| v0.2／2026-09-23 | 登记 D04：不做订单；删除相关方案内容，07 改为库存与外围模块，同步入口、状态模型、配置范围与验证顺序 |
| v0.3／2026-09-23 | 新增 10 的 Function／Effect／State 讨论与 11 的公开资料调研；补 TileV2 效果模型局部证据；登记 A09／Q11，尚未将推荐视为用户已确认实现 |

## 实现顺序建议

1. **Step 1：选择本轮讨论主题。** 当前优先 Q11，并对齐 Q02／Q08；附对应专项文档，不重复询问已确认的 D01–D04。
2. **Step 2：用具体状态例子裁定规则。** 记录用户选择和理由，新建 D 编号；原始需求与源码证据仍保留。
3. **Step 3：同步方案与验收。** 更新受影响章节、配置样例和 08，消除过期的“待确认”及相互矛盾描述。
4. **Step 4：授权实施后按阶段推进。** 执行 08 的下一步，记录真实验证结果和边界；每轮结束更新本页，供下次讨论继续。
