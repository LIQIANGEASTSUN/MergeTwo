# 精简范围与删除记录

## 第一轮清理

只清理 `SeasideEscape_2.6.0_APKPure` 这一安装包的结果目录。清理前逐文件盘点共 985 个文件：437 份 Lua 字节码、437 份反编译 Lua、94 份工具导出的 C# 结构文件、11 个索引和 6 个 Markdown 文件。**没有**图片、音频、模型、动画、AssetBundle 或配置资源文件，因此无需删除这类二进制资源。

本次依据实际方法体、继承/组件装配、静态调用关系和事件路径，保留 182 个完整 Lua 模块及各自原字节码；保留主棋盘、物品组件、生成器、订单、库存/存档、交互入口，以及会改写二合规则的活动棋盘。对是否与规则直接相关存在疑问的模块，优先保留以免误删。相关脚本**没有裁剪、改写、重构或合并**。

删除 609 个旧文件：255 对无关 Lua/字节码、94 个没有真实方法体的 C# 占位文件、4 个随之失效的 C# 索引，以及 1 个下载链接说明。明显无关的范围包括挖掘和清淤独立小游戏、比赛弹窗、大量资源/音效/动画/通用 UI 加载代码、教程执行器和测试自动化。`Drop` 是实际有二合判定的下落玩法，`FreefallActivity`、`CloudsDash`、`HuntActivity`、`HuntMapActivity`、`NoCDTrain` 会覆盖棋盘规则，故其核心代码仍保留。

删除前依据原 `INDEX_MODULES.tsv` 建立逐模块保留清单，逐一确认 `.lua/.luac` 成对存在；364 个保留代码文件的 SHA-256 与原索引逐项一致。第一轮 7 个 Lua/文件索引均已按实际保留文件重建；静态调用索引只记录保留模块之间的边。第一轮清理后目录有 364 个代码文件、7 个索引及 6 个必要说明文档。

本次筛选的目标是可阅读的二合代码参考，而非可执行的全量工程。若后续需要复核被删模块的具体内容，应重新从原安装包拆取；当前给定的 Seaside Escape XAPK 路径已不在磁盘上。

## 第二轮复核（2026-09-24）

按文件完整方法体与保留代码引用再审 182 个模块，进一步删除 4 对共 8 个代码文件：

- `Model.RandomGenerator`：保留脚本中没有任何调用或类型引用，独立随机数工具。
- `Drop.Board.DropActivityBoardView`：文件实际只定义 DropFlyLineEffect，更新飞线的位置、角度与排序。
- `Drop.Board.DropActivityCache`：只对提示、飞线与粒子特效对象进行创建和复用，不包含合成判定或奖励计算。
- `HuntMapActivity.Model.HuntMapActivityTimeLineModel`：猎图剧情时间线及出现动画状态；不定义二合物品、棋盘、消耗或生成规则。

下落玩法的飞行完成回调会执行合成结算，因此保留 `Drop.Board.Item.DropActivityFlyItem` 和承载状态切换、奖励计算的 `Drop.Board.DropActivityBoard`。`HuntMapActivity.Model.HuntMapActivityMapModel`、任务模型及障碍逻辑仍保留其物品消耗、奖励和地图状态关系；剧情时间线只影响演出。删去演出脚本后，保留脚本中的对应调用仍可见，结果目录仍是代码参考，不可直接运行。剩余脚本中夹杂的资源加载、演出或通用方法依照整文件保留原则不裁剪。

第二轮后保留 178 个完整 Lua 模块、356 个代码文件、7 个索引和 6 个说明文档。所有保留代码文件的字节均未修改。
