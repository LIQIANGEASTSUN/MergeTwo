# 工具日志摘录

以下为本次实际运行日志中的关键行（包含精简前的工具处理范围），便于区分成功、失败和工具限制；完整临时日志与资源导出未放入结果目录。

## AssetRipper

首次只载入主 APK：

```text
Import : Files use the 'Unknown' scripting backend.
```

合并主 APK 与 ARM64 分包后重新载入：

```text
Import : Files use the 'IL2Cpp' scripting backend.
Import : During Il2Cpp initialization, found Unity version: 6000.3.22f1
Cpp2IL : Using actual IL2CPP Metadata version 39
Cpp2IL : Processed 216494 OK (117ms)
Export : Finished post-export
```

导入阶段也出现部分序列化布局错误，例如：

```text
Import [Error] : Unable to read MonoBehaviour Structure, because script UI.SplashScreenController layout mismatched binary content (ArgumentOutOfRangeException).
```

## 仓库内 Il2CppDumper

```text
Initializing metadata...
System.NotSupportedException: ERROR: Metadata file supplied is not a supported version[39].
```

## 独立 Cpp2IL

```text
Version 2022.1.0-pre-release.21+58fc404ac503f4e512055cafc48c03088fc6e224
[Info] [Library] 	Using actual IL2CPP Metadata version 39
[Info] [Library] Mapping pointers to Il2CppMethodDefinitions...Processed 216494 OK (799ms)
[Info] [Program] Done.
```

## Ghidra

```text
INFO  ExportMergeMethods.java> Program image base: 00100000 (GhidraScript)
ERROR Function body does not fully contain the call site area (LSDACallSiteTable)
ERROR Failed to disassemble at 055ec248 (GccExceptionAnalyzer)
```

全局分析报错的那一轮被中止；后续成功的伪 C 使用关闭全局分析、独立处理选定方法的流程。保留范围内每个方法的最终状态见 `native/Ghidra-C/status.tsv`。

第二轮延长单方法时限后，97 个目标中 94 个成功，另外 3 个的工具返回分别为 `Response buffer size exceeded for: libil2cpp.so`（两个 `TryToCollectItemFromBoard` 重载）和 `process: timeout`（`TryToUnlockProducerInventorySlot`）。

后续 15 个补充目标均返回了伪 C，使返回伪 C 的目标达到 109 个。但质量初筛发现不少输出跨越 Cpp2IL 指令范围；具体逐方法标记见 `伪C质量初筛.csv`。曾对 `SpawnStorageFull` 强制设置 `036d9b78-036d9cf7` 的 Ghidra 函数体范围，反编译仍输出 2,944 行并引用范围外的控制流标签，因此未将该结果视为可信的独立方法实现。
