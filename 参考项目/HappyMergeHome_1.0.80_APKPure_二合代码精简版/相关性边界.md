# 保留代码的相关性边界

**不能把此目录的每个方法都当成二合实现。**当前筛选单位是完整脚本，共 342 份：按筛选理由，172 份归入主棋盘、覆写棋盘行为的活动变体、特殊产出/状态模型或障碍解锁逻辑；另 170 份是规则配置结构、存档类型、基类、容器和直接依赖。这是代码导航分类，不是对每个方法进行行为验证。

例如 [MergeBoard.cs](code/AssetRipper_generated_CSharp/Assembly-CSharp/MergeBoard.cs) 和 [MergeManager.cs](code/AssetRipper_generated_CSharp/Assembly-CSharp/MergeManager.cs) 是主要实现入口；[TableMergeItem.cs](code/AssetRipper_generated_CSharp/Assembly-CSharp/TableMergeItem.cs) 主要提供字段结构；[StorageList.cs](code/AssetRipper_generated_CSharp/Assembly-CSharp/DragonU3DSDK/Storage/StorageList.cs) 是通用持久化容器，本身不实现合成判定。[SummerWatermelon_21/SummerWatermelonModel.cs](code/AssetRipper_generated_CSharp/Assembly-CSharp/Activity/SummerWatermelon_21/SummerWatermelonModel.cs) 同时有 `TryProductWaterMellon`、`OnNewItem` 和 `PurchasePackageSuccess`、`GiftBagSendOneGetReward` 等方法；因原要求保留整个相关脚本，后两类方法及对应伪 C 也留在目录里。

94 份活动棋盘脚本直接继承或覆写 `MergeBoard`，用于观察玩法变体；它们不等于主线二合的必读代码。逐文件的保留理由见[文件索引](file_index.json)和[筛选清单](筛选清单.csv)。这些理由来自名称、继承/接口、签名引用和静态调用候选，不是运行时证明。原生伪 C 还需结合[完整性说明](完整性说明.md)中的质量状态阅读。

目录不含媒体资产或配置资源值。若研究范围进一步限定为“只看主线合成判定、生成、棋盘状态和存档”，可单独收窄到[代码索引](代码索引.md)的主流程及数据结构；那将排除真实存在的活动玩法变体。
